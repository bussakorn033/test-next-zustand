/**
 * @param id - The HTML `id` attribute for the loading component.
 * @param isLoading - Indicates whether the loading state should be active. When `true`, the loading UI will be shown.
 */

export interface LoadingProps {
	id?: string;
	isLoading?: boolean;
}
